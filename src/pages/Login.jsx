import LoginForm from './../components/LoginForm/LoginForm';
import Cometa from '../components/Cometa/Cometa';

const Login = ({}) => {
  return (
    <div>
      <Cometa />
      <LoginForm />
    </div>
  );
};

export default Login;
